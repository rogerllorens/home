import { Button } from "@/components/ui/Button";
import { Card } from "@/components/ui/Card";
import { Badge } from "@/components/ui/Badge";
export const metadata = { title: "Search Console Opportunity Engine | Rankelia.ai", description: "Prioriza productos con queries reales, CTR bajo, posiciones 4-15 e impresiones sin clics." };
export default function SearchConsolePage() { return <main className="mx-auto max-w-6xl px-4 py-20"><Badge variant="info">Integración readonly</Badge><h1 className="mt-5 text-5xl font-black">Search Console Opportunity Engine</h1><p className="mt-5 max-w-3xl text-lg text-slate-600">Rankelia cruza catálogo, propuestas y métricas reales de GSC para detectar quick wins, CTR bajo y URLs con demanda. La conexión se gestiona dentro de la app privada con OAuth de solo lectura.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{["Queries reales", "Quick wins", "Priorización por producto"].map((item) => <Card key={item}><h2 className="text-xl font-black">{item}</h2><p className="mt-2 text-slate-600">Datos de Search Console enriquecen oportunidades sin publicar ni modificar nada en Google.</p></Card>)}</div><div className="mt-10"><Button href="/login">Entrar y conectar GSC</Button></div></main>; }
