import { createClient } from '@/lib/supabase/server'
import { DeleteAccountDialog } from '@/components/dashboard/DeleteAccountDialog'
export default async function Settings(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data:profile}=await s.from('profiles').select('is_public,username').eq('user_id',user.id).single();return <main className='p-4 max-w-xl mx-auto space-y-4'><form action='/api/profile/check-username' method='post' className='card space-y-2'><input name='username' defaultValue={profile?.username} className='w-full border rounded-xl p-2'/><label className='flex gap-2'><input name='is_public' type='checkbox' defaultChecked={profile?.is_public}/> Perfil público</label><button className='btn-primary'>Guardar ajustes</button></form><DeleteAccountDialog/></main>}
