import { SUBSCRIPTION_PLANS, EXTRA_PRODUCT_PACKS } from "@/lib/pricing";
import { Button } from "@/components/ui/Button";
import { Card } from "@/components/ui/Card";

export const metadata = { title: "Precios | Rankelia.ai", description: "Planes por productos SEO y packs extra para optimizar catálogos ecommerce por CSV, con revisión humana recomendada." };
export default function PricingPage() { return <main className="mx-auto max-w-7xl px-4 py-20"><h1 className="text-5xl font-black">Precios claros por productos SEO</h1><p className="mt-4 max-w-3xl text-lg text-slate-600">Los créditos internos son una unidad técnica. De cara al usuario trabajamos con productos estándar mensuales y packs extra.</p><div className="mt-10 grid gap-5 md:grid-cols-3 lg:grid-cols-5">{SUBSCRIPTION_PLANS.map((plan) => <Card key={plan.id}><h2 className="text-2xl font-black">{plan.name}</h2><p className="mt-2 text-3xl font-black">{plan.price}€</p><p className="mt-2 text-slate-600">{plan.monthlyProducts} productos estándar/mes</p></Card>)}</div><h2 className="mt-14 text-3xl font-black">Productos extra</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{EXTRA_PRODUCT_PACKS.slice(0, 6).map((pack) => <Card key={pack.id}><p className="text-xl font-black">{pack.quantity} productos</p><p className="mt-1 text-slate-600">{pack.price}€</p></Card>)}</div><Button className="mt-10" href="/login?mode=register">Empezar gratis</Button></main>; }
