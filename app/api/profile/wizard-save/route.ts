import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { wizardSchema } from '@/lib/validations/wizard'
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'unauthorized'},{status:401});const body=await req.json();const parsed=wizardSchema.safeParse(body);if(!parsed.success)return NextResponse.json(parsed.error.flatten(),{status:400});const v=parsed.data;const {data:profile,error}=await s.from('profiles').upsert({user_id:user.id,username:v.username,display_name:v.display_name,city:v.city,country:v.country,intention:v.intention,emotional_status:v.emotional_status,bio_short:v.headline,is_public:v.is_public,is_completed:!body.draft},{onConflict:'user_id'}).select().single();if(error)return NextResponse.json({error:error.message},{status:400});await s.from('love_cvs').upsert({profile_id:profile.id,headline:v.headline,applying_for:v.applying_for,about_me:v.about_me,emotional_experience:v.about_me,affective_skills:v.affective_skills,green_flags:v.green_flags,soft_red_flags:v.soft_red_flags,love_languages:v.love_languages,ideal_date:v.ideal_date,availability:v.availability,final_cta:v.final_cta},{onConflict:'profile_id'});return NextResponse.json({ok:true})}
