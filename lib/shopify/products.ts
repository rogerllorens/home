export const SHOPIFY_PRODUCTS_QUERY = `query RankeliaProducts($first: Int!, $after: String) { products(first: $first, after: $after, sortKey: UPDATED_AT) { pageInfo { hasNextPage endCursor } edges { node { id legacyResourceId title handle vendor productType status onlineStoreUrl descriptionHtml seo { title description } tags images(first: 10) { edges { node { id url altText } } } variants(first: 20) { edges { node { id sku title price inventoryPolicy selectedOptions { name value } } } } updatedAt } } } }`;
export type ShopifyProductNode = { id: string; legacyResourceId?: string; title?: string; handle?: string; vendor?: string; productType?: string; status?: string; onlineStoreUrl?: string; descriptionHtml?: string; seo?: { title?: string; description?: string }; tags?: string[]; images?: { edges?: Array<{ node: { id: string; url?: string; altText?: string } }> }; variants?: { edges?: Array<{ node: { id: string; sku?: string; title?: string; price?: string; inventoryPolicy?: string; selectedOptions?: Array<{ name: string; value: string }> } }> }; updatedAt?: string };
