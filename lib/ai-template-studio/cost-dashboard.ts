import { aggregateGenerationCost } from "./cost";
export function buildCostDashboard(runs: Array<{ estimated_cost?: number | null; task_type?: string; model?: string; prompt_version_id?: string | null; fallback_used?: boolean; quality_score?: number | null }>) { const cost = aggregateGenerationCost(runs); const fallbackRate = runs.length ? runs.filter((run) => run.fallback_used).length / runs.length : 0; const avgQuality = runs.length ? runs.reduce((sum, run) => sum + Number(run.quality_score ?? 0), 0) / runs.length : 0; return { ...cost, runs: runs.length, fallbackRate, avgQuality, alerts: [cost.total > Number(process.env.AI_COST_ALERT_DAILY_USD ?? Infinity) ? "daily_cost_threshold" : null, fallbackRate > 0.25 ? "fallback_spike" : null].filter(Boolean) }; }
