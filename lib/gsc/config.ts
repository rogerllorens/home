export type GscConfig = { enabled: boolean; clientId?: string; clientSecret?: string; redirectUri?: string; scopes: string; rowLimit: number; maxRowsPerDimension: number; defaultDays: number; extendedDays: number; dataDelayDays: number; timeoutMs: number };
const num = (key: string, fallback: number) => Number.isFinite(Number(process.env[key])) ? Number(process.env[key]) : fallback;
export function getGscConfig(): GscConfig { return { enabled: process.env.GSC_ENABLED === "true", clientId: process.env.GOOGLE_CLIENT_ID, clientSecret: process.env.GOOGLE_CLIENT_SECRET, redirectUri: process.env.GOOGLE_REDIRECT_URI, scopes: process.env.GOOGLE_GSC_SCOPES || "https://www.googleapis.com/auth/webmasters.readonly", rowLimit: num("GSC_SYNC_ROW_LIMIT", 25000), maxRowsPerDimension: num("GSC_SYNC_MAX_ROWS_PER_DIMENSION", 50000), defaultDays: num("GSC_SYNC_DEFAULT_DAYS", 28), extendedDays: num("GSC_SYNC_EXTENDED_DAYS", 90), dataDelayDays: num("GSC_SYNC_DATA_DELAY_DAYS", 3), timeoutMs: num("GSC_SYNC_TIMEOUT_MS", 60000) }; }
export function assertGscEnabled() { const config = getGscConfig(); if (!config.enabled) throw new Error("gsc_disabled"); if (!config.clientId || !config.clientSecret || !config.redirectUri) throw new Error("gsc_not_configured"); if (config.scopes.split(/\s+/).some((scope) => scope && scope !== "https://www.googleapis.com/auth/webmasters.readonly")) throw new Error("gsc_scope_must_be_readonly"); return config; }
