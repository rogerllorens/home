import assert from "node:assert/strict";
import test from "node:test";
import { buildGscOpportunities } from "../lib/opportunities/gsc-opportunities";
test("builds low CTR and quick win opportunities from real GSC rows", () => { const ops = buildGscOpportunities({ urlMetrics: [{ id: "u1", page_url: "https://example.com/p", impressions: 2300, clicks: 10, ctr: 0.004, position: 7.4, matched_catalog_item_id: "c1", matched_proposal_id: "p1", match_confidence: 1 }], proposals: [{ id: "p1", active_scores: { overall: 54 } }] }); assert.ok(ops.some((op) => op.type === "gsc_high_impressions_low_ctr")); assert.ok(ops.some((op) => op.type === "gsc_quick_win_position_4_15")); assert.equal(ops[0].source, "gsc"); });
test("builds query-not-in-meta opportunity only from page query data", () => { const ops = buildGscOpportunities({ pageQueryMetrics: [{ id: "q1", page_url: "https://example.com/p", query: "taladro bosch profesional", impressions: 1000, clicks: 20, ctr: 0.02, position: 8, matched_proposal_id: "p1" }], proposals: [{ id: "p1", current_snapshot: { meta_title: "Herramienta eléctrica", meta_description: "Producto para bricolaje" } }] }); assert.ok(ops.some((op) => op.type === "gsc_query_not_in_meta")); assert.ok(ops.some((op) => op.type === "gsc_query_not_in_title")); });
