# AI Worker Integration

Prompt 7 adds proposal-version link columns for template, prompt version, sector rule and generation run. Repository inserts now accept provider, template/prompt IDs, AI run ID and token usage metadata so worker/regeneration can persist end-to-end traceability.
