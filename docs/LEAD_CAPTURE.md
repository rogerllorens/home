# Lead Capture

Free audit lead capture stores email only when provided. Sending a report requires explicit report consent. Marketing consent is stored separately and is never enabled by default.

Stored audit data includes hashed IP/user-agent, UTM fields, report timestamps and report events. Public report pages do not expose email, IP or user-agent.
