# Export approved

Approved exports load only `optimization_proposals` with `approved_version_id`, convert approved version output to the existing Rankelia/Shopify/WooCommerce/PrestaShop CSV builders and create private downloads plus HTML/TXT reports.

If there are zero approved versions, the export endpoint returns an error. Rankelia still keeps legacy job exports for backwards compatibility.
