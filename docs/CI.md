# CI

GitHub Actions runs install, typecheck, lint, unit tests, production build, environment validation, whitespace checks and npm audit. A Supabase local integration placeholder is included for a manual workflow once Docker/Supabase CLI setup is available in CI.
