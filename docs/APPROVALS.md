# Approvals

Approval is explicit. A user can approve only a version that belongs to their proposal. If a version requires human review, the API expects explicit confirmation. Approved proposals become eligible for approved-only exports; pending proposals are never included in that flow.
