# Pasted Table Import

Users can paste rows copied from Excel, Google Sheets or an ERP into `/app/upload`. Rankelia detects tab, semicolon, comma or pipe delimiters, parses the table, applies column mapping confidence and normalizes the data to the same UTF-8 CSV job format.

This is not a full spreadsheet editor: formulas, merged cells and workbook formatting are not supported.
