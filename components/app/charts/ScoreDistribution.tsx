export function ScoreDistribution({ low, medium, high }: { low: number; medium: number; high: number }) { const total = Math.max(low + medium + high, 1); return <div aria-label="Distribución de scores" className="flex h-3 overflow-hidden rounded-full bg-slate-100"><div className="bg-rose-500" style={{ width: `${(low / total) * 100}%` }} /><div className="bg-amber-500" style={{ width: `${(medium / total) * 100}%` }} /><div className="bg-emerald-500" style={{ width: `${(high / total) * 100}%` }} /></div>; }
