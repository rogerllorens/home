export function Sparkline({ values }: { values: number[] }) { const safe = values.length ? values : [0]; const max = Math.max(...safe, 1); const points = safe.map((value, index) => `${(index / Math.max(safe.length - 1, 1)) * 100},${28 - (value / max) * 24}`).join(" "); return <svg aria-label="Tendencia" viewBox="0 0 100 32" className="h-8 w-full"><polyline fill="none" stroke="#2563eb" strokeWidth="4" points={points} strokeLinecap="round" strokeLinejoin="round" /></svg>; }
