export function ScoreRing({ value, label }: { value: number | null | undefined; label: string }) { const safe = Math.max(0, Math.min(100, Math.round(Number(value ?? 0)))); const deg = safe * 3.6; return <div aria-label={`${label}: ${safe || "sin datos"}`} className="relative grid place-items-center"><div className="h-28 w-28 rounded-full" style={{ background: `conic-gradient(#2563eb ${deg}deg, #e2e8f0 ${deg}deg)` }}><div className="m-3 grid h-[5.5rem] w-[5.5rem] place-items-center rounded-full bg-white"><span className="text-2xl font-black">{value == null ? "—" : safe}</span></div></div><p className="mt-2 text-center text-xs font-bold text-slate-500">{label}</p></div>; }
