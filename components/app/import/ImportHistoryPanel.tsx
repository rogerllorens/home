"use client";
import { Badge } from "@/components/ui/Badge";
import { Button } from "@/components/ui/Button";
import { Card } from "@/components/ui/Card";
export type ImportRunSummary = { id: string; source_type: string; original_file_name?: string | null; sheet_name?: string | null; status: string; rows_total: number; rows_valid: number; rows_invalid: number; mapping_confidence?: number | null; job_id?: string | null; created_at: string };
export function ImportHistoryPanel({ runs }: { runs: ImportRunSummary[] }) { return <Card><div className="flex items-center justify-between gap-3"><div><h2 className="text-xl font-black">Historial de importaciones</h2><p className="text-sm text-slate-600">Revisa archivos, warnings y reintenta si existe CSV normalizado.</p></div><Button href="/app/upload">Nueva importación</Button></div><div className="mt-4 grid gap-3">{runs.length ? runs.map((run) => <div className="rounded-2xl border border-slate-200 p-4" key={run.id}><div className="flex flex-wrap items-center gap-2"><Badge variant="info">{run.source_type}</Badge><Badge variant={run.status === "failed" ? "danger" : "success"}>{run.status}</Badge><b>{run.original_file_name ?? "Importación"}</b></div><p className="mt-2 text-sm text-slate-600">{run.rows_valid}/{run.rows_total} filas válidas · inválidas {run.rows_invalid} · hoja {run.sheet_name ?? "—"} · confidence {Math.round(Number(run.mapping_confidence ?? 0) * 100)}%</p><div className="mt-3 flex gap-2">{run.job_id ? <Button href={`/app/jobs/${run.job_id}`} variant="secondary">Ver job</Button> : null}<Button href={`/api/app/import/runs/${run.id}`} variant="ghost">Detalle JSON</Button></div></div>) : <p className="rounded-2xl bg-slate-50 p-4 text-sm font-semibold text-slate-600">Aún no hay importaciones registradas.</p>}</div></Card>; }
