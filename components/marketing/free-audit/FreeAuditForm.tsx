import { FormEvent } from "react";
import { Button } from "@/components/ui/Button";

export function FreeAuditForm({ url, email, consentEmailReport, loading, onUrlChange, onEmailChange, onConsentEmailReportChange, onSubmit }: { url: string; email: string; consentEmailReport: boolean; loading: boolean; onUrlChange: (value: string) => void; onEmailChange: (value: string) => void; onConsentEmailReportChange: (value: boolean) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) {
  return <form className="mt-8 space-y-3" onSubmit={onSubmit}><div className="grid gap-3 lg:grid-cols-[1.2fr_0.8fr_auto]"><label className="sr-only" htmlFor="audit-url">URL de ecommerce</label><input className="min-h-14 rounded-2xl border border-slate-200 px-5 text-base font-semibold outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-100" id="audit-url" onChange={(event) => onUrlChange(event.target.value)} placeholder="https://mitienda.com" required type="text" value={url} /><label className="sr-only" htmlFor="audit-email">Email opcional</label><input className="min-h-14 rounded-2xl border border-slate-200 px-5 text-base outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-100" id="audit-email" onChange={(event) => onEmailChange(event.target.value)} placeholder="Recibir informe por email" type="email" value={email} /><Button className="min-h-14" disabled={loading} type="submit">{loading ? "Analizando…" : "Analizar mi tienda"}</Button></div><label className="flex items-start gap-3 rounded-2xl bg-slate-50 p-3 text-sm font-semibold text-slate-600"><input checked={consentEmailReport} className="mt-1" disabled={!email} onChange={(event) => onConsentEmailReportChange(event.target.checked)} type="checkbox" />Recibir el informe completo por email. No es consentimiento de marketing y Rankelia no publica cambios automáticamente.</label></form>;
}
