'use client'
import { useRouter, useSearchParams } from 'next/navigation'
export function ExploreFilters(){const r=useRouter();const s=useSearchParams();const apply=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const fd=new FormData(e.currentTarget);const q=new URLSearchParams();fd.forEach((v,k)=>v&&q.set(k,String(v)));r.push(`/explorar?${q.toString()}`)};return <form onSubmit={apply} className='card grid grid-cols-2 gap-2'><input name='city' defaultValue={s.get('city')||''} placeholder='Ciudad' className='border rounded p-2'/><input name='country' defaultValue={s.get('country')||''} placeholder='País' className='border rounded p-2'/><input name='intention' defaultValue={s.get('intention')||''} placeholder='Intención' className='border rounded p-2'/><select name='sort' defaultValue={s.get('sort')||'recommended'} className='border rounded p-2'><option value='recommended'>Recomendados</option><option value='featured'>Destacados</option><option value='recent'>Recientes</option><option value='most_viewed'>Más vistos</option></select><button className='btn-primary col-span-2'>Aplicar filtros</button></form>}
