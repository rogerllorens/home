'use client';

export function CaptchaPlaceholder() {
  return (
    <div className="flex items-center justify-center rounded-2xl border border-dashed border-muted/50 bg-muted/30 px-4 py-6 text-xs text-text-muted">
      hCaptcha se cargará aquí en producción
    </div>
  );
}
