'use client'
export function BoostPurchaseCard({profileId}:{profileId:string}){const buy=async(type:string)=>{const r=await fetch('/api/stripe/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({type,profile_id:profileId})});const d=await r.json();if(d.url)location.href=d.url};return <div className='card'><h3 className='font-semibold'>Destaca tu candidatura durante 24h.</h3><div className='flex gap-2 mt-3'><button className='btn-primary' onClick={()=>buy('boost_24')}>Boost 24h · 2,99€</button><button className='btn-secondary' onClick={()=>buy('boost_72')}>Boost 72h · 5,99€</button></div></div>}
