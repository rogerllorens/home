'use client'
export function PricingCard({title,price,features,type}:{title:string;price:string;features:string[];type:string}){const buy=async()=>{const r=await fetch('/api/stripe/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({type})});const d=await r.json();if(d.url)location.href=d.url};return <div className='card'><h3 className='text-xl font-bold'>{title}</h3><p className='text-3xl my-2'>{price}</p><ul className='text-sm space-y-1'>{features.map(f=><li key={f}>• {f}</li>)}</ul><button className='btn-primary mt-4' onClick={buy}>Elegir plan</button></div>}
