'use client'
export function RequestActionButtons({id,isReceiver}:{id:string;isReceiver:boolean}){const act=async(a:string)=>{await fetch(`/api/interview-requests/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action:a})});location.reload()};return <div className='flex flex-wrap gap-2'>{isReceiver&&<><button className='btn-primary' onClick={()=>act('accepted')}>Aceptar entrevista</button><button className='btn-secondary' onClick={()=>act('rejected')}>Rechazar con cariño</button><button className='btn-secondary' onClick={()=>act('archived')}>Guardar para luego</button></>}<button className='btn-secondary' onClick={()=>act('cancelled')}>Cancelar</button></div>}
