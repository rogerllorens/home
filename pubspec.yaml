name: nexus_qr
description: Nexus QR – Generador y Lector Profesional de Códigos
publish_to: 'none'
version: 3.0.0+5

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.5
  qr_flutter: ^4.0.0
  barcode_widget: ^2.0.1
  mobile_scanner: ^3.0.0
  image_gallery_saver: ^1.7.1
  permission_handler: ^10.2.0
  clipboard: ^0.1.3
  url_launcher: ^6.1.10
  share_plus: ^6.3.4
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
