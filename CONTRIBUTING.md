# Contributing to Scanly

Thanks for your interest in contributing! Please ensure the following:

- Lint and tests pass (`npm test`).
- Follow Conventional Commits for commit messages.
- Submit pull requests against `main` with a clear description.

We use feature branches named `feature/<name>` and squash merge when approved.
